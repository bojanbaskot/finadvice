{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && python manage.py createsuperuser --noinput --username admin --email admin@finadvice.com || true && python manage.py collect_mentions || true && gunicorn config.wsgi --workers 2 --threads 2 --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
